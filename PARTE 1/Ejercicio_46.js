
for (let i = 0 ; i < 3; i++){
    num = parseFloat(prompt("Ingrese un numero: "));
    numMayor = num;
    if (numMayor => num) {
        numMayor = num;
    }
}

alert(`El numero mayor es: ${numMayor}`)


